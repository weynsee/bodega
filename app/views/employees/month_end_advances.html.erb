<div class="row">
  <div class="col-lg-12">
    <%= render 'employees/nav_pills' %>
    <div class="tab-content">
      <div class="tab-pane active">
        <%= render 'shared/advances/search_form',
          url: nesting_aware_month_end_advances_path,
          type: :monthly %>
        <%= render 'shared/advances/table' %>
        <div class="row">
          <div class="col-auto mr-auto">
            <nav aria-label="Advances list navigation">
              <%= pagy_bootstrap_nav(@pagy).html_safe %>
            </nav>
          </div>
          <div class="col-auto">
            <%= link_to 'Create new month end advance',
              new_employee_month_end_advance_path(@employee),
              class: 'btn btn-secondary' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
